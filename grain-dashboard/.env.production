# Firebase Hosting rewrites proxy /api/** to Cloud Run — no separate URL needed
# Only set this if you need to point to a different backend
# VITE_API_URL=https://grainbiz-api-euzttsz43q-uc.a.run.app/api


